[build]
dockerfilePath = "services/log-relay/Dockerfile"
watchPatterns = ["services/log-relay/**"]

[deploy]
healthcheckPath = "/health"
healthcheckTimeout = 120
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 5
