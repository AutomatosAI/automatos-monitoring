[build]
dockerfilePath = "services/loki/Dockerfile"
watchPatterns = ["services/loki/**"]

[deploy]
healthcheckPath = "/ready"
healthcheckTimeout = 30
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 5
